# Fixes
- eaksn4: switch back from fullscreen if started in fullscreen

- cleanup texloadbuf in ldresetdo
- load: delete texs for img size reduce

- fix: stop-icon disapears if zoomin and -out on flstln/panorama
- fix: load errors

# Want to have
- dpl.actimgi through eff + remove sdlforceredraw
- dpl.pos.actil through eff + remove sdlforceredraw
- dplgetinput through eff + remove sdlforceredraw
- imgposmark for cat through eff

? thread priorities


- <100 => load exif on dir load
- map: right -> open input for setting pos
- map: right drag => move cluster
