# Fixes
- eaksn4: switch back from fullscreen if started in fullscreen

- cleanup texloadbuf in ldresetdo
- load: delete texs for img size reduce

- fix: stop-icon disapears if zoomin and -out on flstln/panorama

- xinerama => current screen (svn-branch!)

- fix: load errors

- only use thumbs with newer timestamp

# Want to have
- load imgexif threaded

- eff: per value start-time, end-time
- effinit only on changed values and depended on the ev-type
- interate status and cat
- dpl.actimgi through eff
- dpl.showinfo through eff
- dpl.showhelp through eff
- imgposmark for cat through eff
- dpl.pos.actil through eff

? thread priorities
