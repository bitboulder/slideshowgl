# Fixes
- cleanup texloadbuf in ldresetdo
- load: delete texs for img size reduce

# Want to have
- dpl.actimgi through eff + remove sdlforceredraw
- dpl.pos.actil through eff + remove sdlforceredraw
- dplgetinput through eff + remove sdlforceredraw
- imgposmark for cat through eff

? thread priorities

- map title => img / free titles
- diredit in normal write mode (basedir/current dir)
- menu with submenu for active actions
- action register (replace dpl / menu)

